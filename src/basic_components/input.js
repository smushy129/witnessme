import styled from 'styled-components'

const Input = styled.input`
  padding: 4px 8px;
  font-size: 16px;
  placeholder: Search;
  text-align: center;
  outline: none;

  &:focus {
    text-align: left;
  },
`

export default Input
